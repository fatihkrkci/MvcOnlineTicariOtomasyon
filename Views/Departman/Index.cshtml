@using MvcOnlineTicariOtomasyon.Models.Entities
@model List<Departman>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<link rel="stylesheet" href="~/mazer-1.0.0/dist/assets/vendors/simple-datatables/style.css">

<header class="mb-3">
    <a href="#" class="burger-btn d-block d-xl-none">
        <i class="bi bi-justify fs-3"></i>
    </a>
</header>

<div class="page-heading">
    <div class="page-title">
        <div class="row">
            <div class="col-12 col-md-6 order-md-1 order-last">
                <h3>Departman İşlemleri</h3>
                <p class="text-subtitle text-muted">Bu sayfadan departmanlarla ilgili işlemleri gerçekleştirebilirsiniz.</p>
            </div>
            <div class="col-12 col-md-6 order-md-2 order-first">
                <nav aria-label="breadcrumb" class="breadcrumb-header float-start float-lg-end">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="/Istatistik/Index/">Personel Paneli</a></li>
                        <li class="breadcrumb-item active" aria-current="page">Departman İşlemleri</li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
    <section class="section">
        <div class="card">
            <div class="card-header">
                <a href="/Departman/DepartmanEkle/" class="btn btn-primary">Yeni Departman Ekle</a>
            </div>
            <div class="card-body">
                <table class="table table-striped" id="table1">
                    <thead>
                        <tr>
                            <th>Departman Adı</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.DepartmanAd</td>
                                <td>
                                    <a href="/Departman/DepartmanDetay/@item.DepartmanId" class="btn btn-success">Detaylar</a>
                                    <a href="/Departman/DepartmanGetir/@item.DepartmanId" class="btn btn-warning">Güncelle</a>
                                    <a href="/Departman/DepartmanSil/@item.DepartmanId" class="btn btn-danger">Sil</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </section>
</div>

<script src="~/mazer-1.0.0/dist/assets/vendors/simple-datatables/simple-datatables.js"></script>
<script>
    let table1 = document.querySelector('#table1');
    let dataTable = new simpleDatatables.DataTable(table1, {
        searchable: true,
        fixedHeight: true,
        perPage: 5,
        labels: {
            placeholder: "Ara...",
            noRows: "Gösterilecek kayıt bulunamadı",
            info: "Toplam {rows} kayıttan {start} ve {end} arasındakiler gözüküyor"
        }
    });
</script>